{
  "name": "screeps-ai-v03",
  "version": "0.1.0",
  "private": true,
  "description": "Screeps: World project using a TypeScript starter layout.",
  "scripts": {
    "build": "node ./scripts/build.js",
    "typecheck": "tsc --noEmit",
    "watch": "tsc --noEmit --watch",
    "upload": "node ./scripts/upload-screeps.js",
    "deploy": "npm run build && npm run upload"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.9",
    "@types/screeps": "^3.3.8",
    "typescript": "^5.8.3"
  }
}
