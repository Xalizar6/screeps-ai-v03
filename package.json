{
  "name": "screeps-ai-v03",
  "version": "0.1.0",
  "private": true,
  "description": "Screeps: World project using a TypeScript starter layout.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "watch": "tsc --noEmit --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "fix": "npm run lint:fix && npm run format",
    "build": "npm run lint && npm run format:check && npm run typecheck && node ./scripts/build.js",
    "upload": "node ./scripts/run-upload.js",
    "deploy": "npm run build && npm run upload"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/node": "^24.0.0",
    "@types/screeps": "^3.3.8",
    "esbuild": "^0.25.9",
    "eslint": "^9.39.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.46.0"
  }
}
